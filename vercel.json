{
  "installCommand": "npm install",
  "buildCommand": "npm run build -w snap-frame-frontend",
  "outputDirectory": "apps/frontend/.next",
  "functions": {
    "api/index.ts": {
      "includeFiles": "apps/backend/**",
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
