{
  "installCommand": "npm install",
  "buildCommand": "if [ -d apps/frontend ]; then cd apps/frontend && npm run build && mv .next ../../.next; elif [ -d frontend ]; then cd frontend && npm run build && mv .next ../.next; else npm run build && mv .next ./next_backup; fi",
  "outputDirectory": ".next",
  "functions": {
    "api/index.ts": {
      "includeFiles": "apps/backend/**",
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
